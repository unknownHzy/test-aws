{
  "name": "test",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "start": "npm run migrate && npx sequelize db:seed:all && node server.js --inspect",
    "migrate": "npx sequelize-cli db:migrate",
    "test": "mocha --timeout 60000"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "mysql2": "^3.6.1",
    "sequelize": "^6.33.0"
  },
  "devDependencies": {
    "assert": "^2.1.0",
    "mocha": "^10.2.0",
    "sequelize-cli": "^6.6.1"
  }
}
